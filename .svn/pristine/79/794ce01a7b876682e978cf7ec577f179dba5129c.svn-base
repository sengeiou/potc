package com.poct.android.receiver;
import android.annotation.SuppressLint;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;


@SuppressLint("NewApi")
public class AttachmentDownloadReceiver extends BroadcastReceiver{

	public static final String UPDATE_ATTACHMENT = "update_attachment";
	public static final String FINISH_ATTACHMENT = "finish_attachment";
	public static final String ATTACHMENT_FAIL= "attachment_fail";

	@Override
	public void onReceive(Context context, Intent intent) {
		// TODO Auto-generated method stub
		if (intent.getAction().equals(UPDATE_ATTACHMENT)){

		}
		else if (intent.getAction().equals(FINISH_ATTACHMENT))
		{

		}
		else if (intent.getAction().equals(ATTACHMENT_FAIL))
		{

		}
	}

}
